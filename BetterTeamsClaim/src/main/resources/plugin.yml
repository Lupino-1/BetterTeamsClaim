name: BetterTeamsClaim
version: '${project.version}'
main: com.lupino.betterteamsclaim.BetterTeamsClaim
api-version: '1.21'
commands:
  claim:
    description: /claim
    permission: claim
depend:
  - BetterTeams
permissions:
  betterteamsclaim.admin:
    default: op
  claim:
    default: true